const validationMessages = {
  comparePassword: 'Passwords must match.',
  password:
    'Must contain at least 8 characters, 1 uppercase, 1 lowercase, 1 special character (ex: !@#$%)',
};

export default validationMessages;
