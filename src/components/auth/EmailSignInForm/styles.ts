import { theme } from '@/constants';

const styles = {
  container: {
    width: 408,
    marginTop: '16px',
  },
  form: {
    display: 'flex',
    flexDirection: 'column',
    gap: '4px',
  },
} as const;

export default styles;
